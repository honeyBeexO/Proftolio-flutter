class Networking {
  static Future<void> launchURL({final String url}) async {
    // try {
    //   if (await canLaunch(url)) {
    //     print('will launch facebook');
    //     return await launch(url, enableJavaScript: true);
    //   }
    // } catch (e) {
    //   return null;
    // }
  }
}
